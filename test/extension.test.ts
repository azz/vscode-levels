import * as assert from 'assert';
import * as vscode from 'vscode';
import * as myExtension from '../src/extension';

suite("Levels Extension Tests", () => {
    test("Has no tests yet!", () => {

    });
});